spring:
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: s3cr3t
  application:
    name: insignis-apigateway
zuul:
  routes:
    supplier-service:
      service-id: insignis-supplier